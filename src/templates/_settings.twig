{% import "_includes/forms" as forms %}

{{ forms.autosuggestField({
    label: "Server-side API URL"|t('collections-proxy'),
    instructions: "Base URL used by the `{% collectionDocument %}`, `{% collectionDocuments %}`, and `{% collectionSearch %}` Twig tags. Safe to be an internal-only hostname.",
    id: 'serverApiUrl',
    name: 'serverApiUrl',
    value: settings.serverApiUrl,
    suggestEnvVars: true,
    suggestAliases: true,
    required: true,
}) }}

{{ forms.autosuggestField({
    label: "Public API URL"|t('collections-proxy'),
    instructions: "Base URL exposed to the browser for client-side search calls.",
    id: 'publicApiUrl',
    name: 'publicApiUrl',
    value: settings.publicApiUrl,
    suggestEnvVars: true,
    suggestAliases: true,
    required: true,
}) }}

{{ forms.autosuggestField({
    label: "Default index"|t('collections-proxy'),
    instructions: "Default index name (e.g. `famsf-artworks-develop`).",
    id: 'index',
    name: 'index',
    value: settings.index,
    suggestEnvVars: true,
    required: true,
}) }}

<p class="light" style="margin-top: 1rem;">
    {{ "`titleField` and `itemFields` are read from `config/collections-proxy.php` (or the equivalent env vars) — they're developer config, not site-admin settings, so they're not editable here."|t('collections-proxy')|md }}
</p>
