{% import "_includes/forms" as forms %}

{{ forms.autosuggestField({
    label: "Server-side API URL"|t('collections-proxy'),
    instructions: "Base URL used by the `{% collectionDocument %}` Twig tag and PHP `ApiClient` service. Safe to be an internal-only hostname.",
    id: 'serverApiUrl',
    name: 'serverApiUrl',
    value: settings.serverApiUrl,
    suggestEnvVars: true,
    suggestAliases: true,
    required: true,
}) }}

{{ forms.autosuggestField({
    label: "Public API URL"|t('collections-proxy'),
    instructions: "Base URL exposed to the browser for client-side Searchkit calls.",
    id: 'publicApiUrl',
    name: 'publicApiUrl',
    value: settings.publicApiUrl,
    suggestEnvVars: true,
    suggestAliases: true,
    required: true,
}) }}

{{ forms.autosuggestField({
    label: "Default index"|t('collections-proxy'),
    instructions: "Default index name (e.g. `famsf-artworks-develop`).",
    id: 'index',
    name: 'index',
    value: settings.index,
    suggestEnvVars: true,
    required: true,
}) }}

<hr>

{{ forms.textField({
    label: "Title field"|t('collections-proxy'),
    instructions: "Document field used as the item page heading (e.g. `title`). Searchkit's own `search_attributes` are configured client-side.",
    id: 'titleField',
    name: 'titleField',
    value: settings.titleField,
}) }}

{{ forms.textField({
    label: "Item fields"|t('collections-proxy'),
    instructions: "Comma-separated fields returned by the `{% collectionDocument %}` Twig tag on item pages. Passed to the API's `?fields=` param. Leave empty for all.",
    id: 'itemFields',
    name: 'itemFields',
    value: settings.itemFields,
}) }}
